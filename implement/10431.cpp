#include <iostream>

using namespace std;

int t,min = 0;
int result[1001];
int line[20];

void GetWalk(){
    
}

int main(){
    cin >> t;

    for(int i = 0; i < t; i++){
        int idx;
        cin >> idx;
        for(int j = 0; j < 20; j++){
            cin >> line[j];
        }
    }

    return 0;
}